# Вопросы к claudeai-test-project-k

## 1. Какова дальнейшая судьба репозитория: он ещё нужен как песочница или его миссия выполнена?

Один коммит от 01.02.2026, план развития из README не выполнен, но описание репозитория правилось позже (~28.02.2026) — значит, владелец к нему возвращался.

**Варианты ответов**
- А. Миссия выполнена, репозиторий больше не нужен.
- Б. Планирую продолжать тесты Claude AI / Desktop Commander именно здесь.
- В. Пусть лежит как памятка о проблеме кодировки.

**Варианты дальнейших действий**
- При А: перенести одностраничный вывод теста в info30/info200 и заархивировать (или удалить) репозиторий — см. сценарий 4 рекомендаций.
- При Б: развить в «юникод-полигон» с CI-матрицей по трём ОС (сценарий 2), чтобы каждый тест оставлял автоматический результат.
- При В: минимум — записать выводы в README и включить GitHub Archive, чтобы «памятка» была самодостаточной.

## 2. Моджибейк в сообщении коммита — случайный дефект или намеренная часть теста?

Сообщение `Initial commit: ð┤ð¥ð▒ð░ð▓ð╗ðÁð¢…` — это UTF-8 «добавлен…», прочитанный как CP437/CP866. Описание репозитория упоминает «проблемы с кодировкой», но неясно, ожидался ли этот результат.

**Варианты ответов**
- А. Случайность: Windows-консоль испортила строку, заметил уже на GitHub.
- Б. Намеренно: специально проверял, пройдёт ли кириллица через `git commit -m`.
- В. Не знаю точно, в каком окружении это произошло.

**Варианты дальнейших действий**
- При А: задокументировать причину и обход (chcp 65001, `git commit -F`, настройки LANG/i18n.commitEncoding) в README и в базе знаний info30 — это ценная находка для всех агентских проектов профиля.
- При Б: значит эксперимент удался — оформить результат как первую запись «базы известных отказов» (идея сочетания №1) и продолжить серию проверок (имена файлов, вывод консоли, переменные окружения).
- При В: воспроизвести: повторить коммит с кириллицей из того же окружения с включённым логированием (`chcp`, `echo %LANG%`), чтобы зафиксировать точную конфигурацию, при которой возникает баг.

## 3. Что означает суффикс «-k»? Существовала ли серия тестовых проектов (a, b, … j)?

В публичном профиле других репозиториев `claudeai-test-project-*` не видно (не проверено исчерпывающе: возможны приватные или удалённые).

**Варианты ответов**
- А. Да, была серия одноразовых тестов; остальные удалены или приватны.
- Б. Нет, буква случайна / сгенерирована инструментом.
- В. Серия планировалась, но дальше «k» не пошло.

**Варианты дальнейших действий**
- При А: собрать выводы всех тестов серии в один индекс (в info200 как хабе вики), иначе результаты потеряны вместе с удалёнными репозиториями.
- При Б: переименовать репозиторий во что-то говорящее (например, `unicode-toolchain-test`), чтобы имя отражало содержание.
- При В: зафиксировать в README, что серия закрыта, и что именно успели проверить.

## 4. Почему ни один пункт чек-листа «Что дальше» не выполнен — потерял актуальность или не дошли руки?

Чек-лист README обещает тесты, requirements.txt, git и GitHub; последние два фактически сделаны, но не отмечены, первые три — нет.

**Варианты ответов**
- А. Чек-лист был шаблонной вежливостью ИИ-ассистента, всерьёз не планировался.
- Б. Планы актуальны, просто низкий приоритет.
- В. Планы переехали в другие репозитории (например, info100/info20).

**Варианты дальнейших действий**
- При А: удалить чек-лист или заменить разделом «Результаты теста» — README перестанет обещать несуществующее.
- При Б: выполнить всё за один заход (~час работы: тест, LICENSE, CI) и получить эталонный минимальный репозиторий (сценарий 3).
- При В: поставить в README ссылку на репозиторий-преемник, чтобы читатель не искал продолжение вслепую.

## 5. Стоит ли перенести главный вывод (баг кириллицы в git-коммитах под Windows) в системную базу знаний профиля?

В профиле есть info30 «Claude Code Knowledge System» и info20 «расширение возможностей Claude Code» — естественные места для таких находок.

**Варианты ответов**
- А. Да, такие находки должны жить в базе знаний, а не в песочницах.
- Б. Нет, пусть каждый тест-репозиторий хранит свои выводы сам.
- В. Базы знаний ещё не готовы принимать такие записи.

**Варианты дальнейших действий**
- При А: создать в info30 запись «кириллица в git commit -m (Windows): симптом/причина/обход» со ссылкой сюда как на живой репродьюсер.
- При Б: тогда обязательно дописать выводы в README этого репозитория — сейчас их нет нигде.
- При В: начать именно с этого кейса как с пилотной записи — он маленький, воспроизводимый и уже публично зафиксирован.
