# Вопросы к info4

## 1. Какое число скиллов каноническое — 87, 88, 101 или 102?

Мастер-индекс говорит о 87, самоаудит — о 88 `.skill`, папка `skills/` содержит 101, MEGA-архив заявляет 102. Единого источника правды нет.

**Варианты ответов:**
- А. Канон — 101 (содержимое `skills/`), остальные числа — исторические срезы.
- Б. Канон — 87 из мастер-индекса, остальное — дубли/вариации одних и тех же скиллов (например, `unit-converter` и `unit-converter-pro`).
- В. Точного ответа нет, никто не сверял.

**Варианты дальнейших действий:**
- При А: обновить мастер-индекс и README до 101, пометить старые отчёты как хронику.
- При Б: провести дедупликацию `skills/other/` (29 «прочих» — главный подозреваемый), слить дубли, зафиксировать канон в `catalog.json`.
- При В: написать скрипт-инвентаризатор (распаковать все `.skill`, сверить `name` из фронтматтера, найти дубли) — полдня работы, закрывает вопрос навсегда.

## 2. Действительно ли все 87/101 скиллов описаны на 100 %, как заявляет PROJECT_COMPLETE, если мастер-индекс показывает 21 %?

`PROJECT_COMPLETE_87_SKILLS_100_PERCENT.md` и BATCH-отчёты заявляют полное покрытие, но `00_MASTER_INDEX_Skills_Catalog.md` остановился на «18/87, 21 %».

**Варианты ответов:**
- А. Да, 100 % описаны — индекс просто не обновляли после первого «чата».
- Б. Описаны частично: поздние партии описаны кратко/шаблонно, «100 %» — формальность.
- В. Неизвестно — отчёты генерировались автоматически и не перепроверялись.

**Варианты дальнейших действий:**
- При А: перегенерировать индекс из фактического содержимого `.skill` файлов (скриптом) и закрыть расхождение.
- При Б: разметить в каталоге статус качества каждого скилла (детально/кратко/заглушка) и допилить приоритетные.
- При В: выборочная ручная проверка 10 случайных скиллов из поздних партий; по результатам — план А или Б.

## 3. Планируется ли реализация Method DB / внешнего сервиса, и какой вариант выбран?

~3 200 строк архитектурной документации (VARIANT_1, VARIANT_2, гибрид) не имеют ни строчки кода. Это главная идея репозитория («псевдо RAG» вынесен в описание на GitHub).

**Варианты ответов:**
- А. Да, Вариант 2 (Method DB) — он проще и не требует инфраструктуры.
- Б. Да, Вариант 1 (внешний сервис) — цель платформа/монетизация.
- В. Нет, это было концептуальное упражнение; реализация не планируется.
- Г. Реализация задумана в другом репозитории профиля (например, info40).

**Варианты дальнейших действий:**
- При А: MVP-индексатор SKILL.md + FTS5-поиск + CLI (см. recommendations.md), затем MCP-обёртка для использования из Claude Code.
- При Б: начать не с K8s-платформы, а с одного FastAPI-эндпоинта `/search`; масштабный документ сократить до roadmap.
- При В: пометить VARIANT_*-доки как «концепт, не реализуется», чтобы статус «Полная документация» не вводил в заблуждение.
- При Г: связать репозитории явными ссылками и перенести архитектурные доки туда, где будет код.

## 4. Проверялись ли скиллы в реальном Claude, и соответствуют ли они действующему формату Agent Skills?

В репозитории нет ни одного примера/лога реального применения скилла. Формат (ZIP с SKILL.md и фронтматтером name/description) похож на формат Anthropic, но соответствие текущим требованиям не подтверждено.

**Варианты ответов:**
- А. Да, часть скиллов использовалась в claude.ai/Claude Code и работала.
- Б. Нет, скиллы генерировались «на вырост» без проверки.
- В. Проверялись только скиллы миграции чатов (chat-migration-*).

**Варианты дальнейших действий:**
- При А: задокументировать 3–5 работающих кейсов (скилл → запрос → результат) в README — это резко поднимет доверие.
- При Б: выбрать 5 скиллов уровня 1, установить в Claude Code, починить формат по результатам, оформить чек-лист соответствия и прогнать по остальным.
- При В: выделить проверенную линию в отдельный «поддерживаемый» раздел, остальное пометить как непроверенное.

## 5. Почему README.md так и не заменён на README_NEW.md, хотя это пункт №1 собственного плана?

Все PR слиты, `FINAL_STATUS_COMPLETE.md` объявляет «полностью готово», но лицо репозитория — по-прежнему две строки «# info4 / info».

**Варианты ответов:**
- А. Забыли/руки не дошли — работа оборвалась после мержа.
- Б. Сознательно: README_NEW требовал правок (там 87 скиллов и «Progress 21 %»).
- В. Ждали ревью человеком, которое не состоялось.

**Варианты дальнейших действий:**
- При А: выполнить замену сейчас (5 минут) с актуализацией цифр.
- При Б: обновить цифры в README_NEW из фактического каталога и заменить.
- При В: провести это ревью (данный аудит фактически его заменяет) и завершить шаг.

## 6. Какова роль «фантастических» версий v4.0 Quantum / v5.0 Cosmic / v6.0 Infinity?

CHANGELOG честно называет их vision/art, но корневой `SKILL.md` (chat-migration-bridge-v45) подаёт «115 функций, 39 % real implementations, квантовые алгоритмы» без такого дисклеймера.

**Варианты ответов:**
- А. Это художественно-концептуальный слой, осознанная часть проекта.
- Б. Это черновики-фантазии, которые не должны были попасть в основную ветку.
- В. Частично реальны: у v3.5 есть практическое содержание, v4+ — экстраполяция.

**Варианты дальнейших действий:**
- При А: собрать их в `docs/vision/` с явным дисклеймером в каждом файле и убрать `SKILL.md` из корня.
- При Б: удалить из основной ветки, оставить в истории git или в `_archive/`.
- При В: провести границу «реализовано/задумано» внутри каждого документа (таблица функций с пометками), чтобы пользователь мог доверять хотя бы части.
