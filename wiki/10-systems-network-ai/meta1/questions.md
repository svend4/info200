# Вопросы к meta1

## 1. Какова цель этой копии continuum-runtime: изучение, вклад в upstream или собственная ветка развития?

Репозиторий содержит ссылку на первоисточник (Asouei/continuum-runtime) и при этом 14 версий собственных расширений, которые никуда не отправлены и не опубликованы.

**Варианты ответов:**
- А. Учебное зеркало — хотел разобраться, как устроен детерминированный рантайм.
- Б. Полигон: проверял, сколько связных фич Claude Code может сгенерировать поверх чужой базы.
- В. Задел под собственный форк/продукт.
- Г. Планировался вклад в upstream, но руки не дошли.

**Варианты дальнейших действий:**
- При А: зафиксировать выводы конспектом в вики (info200), репозиторий заархивировать.
- При Б: оформить эксперимент как кейс-стади (прогнать тесты, посчитать долю рабочих фич) — ценный материал для info30/info20.
- При В: переименовать пакет, обновить авторство и README, добавить CI, публиковать (см. recommendations.md, сценарий 2).
- При Г: отобрать 3–5 лучших подсистем и отправить PR-ами автору upstream (сценарий 1).

## 2. Прогонялась ли тестовая сюита (~150 файлов vitest) хоть раз целиком, и какова доля зелёных тестов?

Тесты написаны на каждый модуль, но CI нет, и аудит не смог проверить их прохождение.

**Варианты ответов:**
- А. Да, всё зелёное — Claude гонял тесты в каждой сессии перед коммитом.
- Б. Гонялись частично/в начале, финальное состояние неизвестно.
- В. Не прогонялись ни разу.

**Варианты дальнейших действий:**
- При А: добавить CI-бейдж и зафиксировать это в README — сразу поднимет доверие и зрелость до 4/5.
- При Б: одна сессия Claude Code: `npm ci && npm test`, починить упавшее, включить GitHub Actions.
- При В: считать v3.1–v4.4 непроверенным кодом; начать с smoke-теста e2e-сценария из README (run → replay → heal) и только потом решать судьбу остальных фич.

## 3. Почему package.json остался 3.0.0, когда CLI объявляет 4.4.0 — недосмотр или намеренно (чтобы не конфликтовать с npm-пакетом upstream)?

**Варианты ответов:**
- А. Недосмотр генеративного конвейера: Claude бампал только строку в CLI.
- Б. Намеренно: имя пакета чужое, публиковаться не собирался, версия пакета неважна.
- В. Не задумывался об этом.

**Варианты дальнейших действий:**
- При А: исправить одним коммитом; заодно добавить проверку согласованности версий в тест.
- При Б: тогда переименовать пакет в package.json (например, `@svend4/continuum-extended`) — исчезнет и конфликт, и двусмысленность авторства.
- При В: решить вопрос вместе с вопросом №1 (цель репозитория определяет политику версий).

## 4. Нужны ли локальному однопользовательскому CLI функции уровня enterprise — RBAC, очередь запусков, rate limiter, вебхуки?

В v4.1–v4.4 добавлены подсистемы, характерные для многопользовательского сервера, при нуле реальных пользователей.

**Варианты ответов:**
- А. Да — прицел на серверный/командный режим работы в будущем.
- Б. Нет — это следствие инерции генерации «ещё пять фич за коммит».
- В. Частично: что-то пригодится (checkpoints, metrics), что-то лишнее (RBAC).

**Варианты дальнейших действий:**
- При А: спроектировать серверный режим (демон + API), иначе RBAC бессмыслен; это большой отдельный этап.
- При Б: вынести спорные подсистемы в отдельные модули-плагины через уже существующий `plugin.ts` или удалить, сократив ядро.
- При В: провести ревизию фич (таблица «оставить/плагин/удалить») и почистить публичный API в `src/index.ts`.

## 5. Планируется ли связать Continuum с другими проектами профиля (info40 — оркестрация агентов, daten4 — планировщик, info100 — лаборатория агентов)?

Continuum по сути — исполнительный слой с верификацией, которого не хватает агентским проектам профиля.

**Варианты ответов:**
- А. Да, именно как общий рантайм для агентов.
- Б. Нет, проекты независимы.
- В. Идея интересна, но не приоритетна.

**Варианты дальнейших действий:**
- При А: начать с самого узкого интеграционного шва — библиотечный вызов `run()/replay()` из оркестратора info40; описать контракт ExecutionPlan в общей вики.
- При Б: тогда довести meta1 до самостоятельной ценности (вопросы 1–2) или заархивировать.
- При В: записать интеграционную идею в info200 как отложенный проект с оценкой трудозатрат (~1–2 сессии на прототип).

## 6. Почему между разработкой (26–27.02.2026) и merge PR #1 (18.04.2026) прошло почти два месяца, и что означает этот merge — «принято, завершено» или «принято, продолжим»?

**Варианты ответов:**
- А. Забыл про ветку, merge — просто уборка.
- Б. Сознательная пауза: решал, что делать с результатом, merge — фиксация «как есть».
- В. Merge — подготовка к следующему этапу, который ещё не начался.

**Варианты дальнейших действий:**
- При А/Б: проставить статус в README («experimental snapshot, не сопровождается») — честно для случайных посетителей, которых README зазывает командой `npm install -g`.
- При В: составить дорожную карту следующего этапа (тесты → CI → судьба upstream) и запустить первую сессию.
