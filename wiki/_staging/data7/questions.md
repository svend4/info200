# Вопросы к data7

## 1. Какой из шести подпроектов главный, а какие — побочные ветки исследования?

Монорепо содержит TSP-оптимизатор диссертаций, трансформатор знаний, MMO RPG-системы, MMO AI Bridge, Meta-Orchestrator Switchboard и профессиональные симуляторы. Ресурсов на все шесть явно не хватит.

**Варианты ответов:**
- А. Главное — научное направление (диссертации + трансформация знаний), остальное — демонстрации метода.
- Б. Главное — Meta-Orchestrator / мультиагентная платформа, наука была разминкой.
- В. Главное — MMO AI Bridge как продуктовая идея (есть бизнес-план и ответ на Хабр).
- Г. Всё это единое исследование «TSP как универсальный метод», разделять нельзя.

**Варианты дальнейших действий:**
- При А: реализовать сценарий A из рекомендаций (валидация на реальных корпусах, pip-пакет, публикация); MMO/оркестратор перевести в docs-архив.
- При Б: слить с info40/info100 (сценарий B), TSP-часть оформить как библиотеку-зависимость.
- При В: довести `mmo_ai_bridge_web` до публичного демо (сценарий C) и опубликовать HABR_ARTICLE_RESPONSE.md.
- При Г: оформить workspace-структуру монорепо с независимыми CI-джобами и общей библиотекой `tsp_toolkit`, README-карту всего исследования.

## 2. Какому статус-отчёту верить: «Production Ready 95%» (README) или «production code 0%, зрелость 40%» (PROJECT_STATUS_v8.0.md)?

За 4–5 февраля 2026 статусы прыгали 40% → 48% → 91% → 100% в противоречащих друг другу документах.

**Варианты ответов:**
- А. Верить PROJECT_STATUS_v8.0 — это прототипы; проценты в README писал ИИ-ассистент «для настроения».
- Б. После спринта 5 февраля код действительно готов — README отражает финал.
- В. Проценты относятся к разным подпроектам, поэтому противоречия нет.
- Г. Сам не знаю реального статуса — нужен независимый аудит.

**Варианты дальнейших действий:**
- При А: удалить/пометить устаревшие отчёты, убрать «Production Ready»-бейджи, ввести единый STATUS.md — репутационная гигиена важнее красивых цифр.
- При Б: доказать делом — прогнать CI до зелёного, развернуть демо, приложить логи прохождения тестов и результатов симуляций.
- При В: в каждом отчёте явно указать подпроект и дату в шапке; собрать сводную таблицу статусов в корневом README.
- При Г: прогнать `pytest backend/tests/`, `mypy`, docker-compose up на чистой машине и зафиксировать фактический статус; далее — план по разрыву между заявленным и реальным.

## 3. Запускались ли когда-нибудь backend, frontend и CI целиком — и что произошло?

В репозитории есть полный DevOps-контур (CI, Docker, мониторинг), но нет ни релизов, ни артефактов запуска; бейджи в README — статические.

**Варианты ответов:**
- А. Да, всё поднималось локально через `make up`, просто не задокументировано.
- Б. Поднимались отдельные части (backend без Redis/Celery), фронтенд не интегрирован.
- В. Нет, код писался ИИ-ассистентом без реального запуска.

**Варианты дальнейших действий:**
- При А: записать это — скриншоты, `docker compose ps`, вывод тестов — в `docs/validation/`; включить реальный бейдж Actions.
- При Б: синхронизировать docker-compose с main.py (redis/celery), составить список «что реально работает» и закрыть разрыв по одному сервису за итерацию.
- При В: начать с минимального smoke-запуска (`uvicorn app.main:app` + sqlite/без Redis), чинить импорты по мере падений; до этого не употреблять слов «Production Ready».

## 4. Планируется ли валидация алгоритмов на реальных данных, а не на симуляциях?

Все заявленные эффекты (−60% выгорания, +36% retention, −40% когнитивной нагрузки) получены на синтетических симуляциях или являются экстраполяциями.

**Варианты ответов:**
- А. Да, есть доступ к реальному корпусу текстов (например, свой архив или lorenzo/Svyazi 2.0).
- Б. Да, для игровой части — планируется мини-игра/бот с реальными пользователями.
- В. Нет, проект сознательно теоретический.

**Варианты дальнейших действий:**
- При А: эксперимент «до/после» на 5–10 реальных документах, публикация методики и результатов; это сильнейший возможный апгрейд проекта.
- При Б: вычленить BurnoutDetector/AIDirector в лёгкий SDK и встроить в простую браузерную игру (можно на базе daten1 — «игры»); собирать реальные метрики сессий.
- При В: переформулировать README всех подпроектов как «теоретическое исследование с прототипами» и убрать продуктовые обещания (ARPU, ROI, retention).

## 5. Почему TSP-алгоритмы реализованы дважды — в корне и в backend/app/services/tsp_algorithms.py?

Корневой `dissertation_optimizer.py` (715 строк) и сервисный `tsp_algorithms.py` (1494 строки) содержат пересекающиеся наборы алгоритмов (2-opt, SA, GA) без общего кода.

**Варианты ответов:**
- А. Историческая случайность: сервис писался позже с нуля, дублирование не осознавалось.
- Б. Намеренно: корневой — учебный/автономный без зависимостей, сервисный — расширенный для API.
- В. Планировалась общая библиотека, руки не дошли.

**Варианты дальнейших действий:**
- При А или В: выделить единый пакет `tsp_toolkit` (pyproject.toml), обе стороны переводятся на импорт из него; дублирующий код удалить.
- При Б: задокументировать это решение в README обоих модулей и добавить перекрёстные тесты, что оба дают согласованные решения на одинаковых входах.

## 6. Какова судьба «диффузионных» концептов (DIFFUSION_LLM_INTEGRATION, DIFFUSION_VFX_RENDERING, DIFFUSION_META_ORCHESTRATOR)?

Шесть объёмных документов описывают интеграцию диффузионных моделей, но по PROJECT_STATUS_v8.0 их прототипы готовы на 20–40%, а кода в репозитории не видно.

**Варианты ответов:**
- А. Это исследовательские эссе «на вырост», реализация не планируется.
- Б. Планируется реализация после стабилизации ядра.
- В. Идеи переехали/переедут в другой репозиторий профиля.

**Варианты дальнейших действий:**
- При А: перенести в `docs/research/` с пометкой «концепт, не реализовано» — сейчас они визуально увеличивают «объём проекта».
- При Б: завести на каждый концепт issue с критериями MVP и не смешивать с текущими фазами Meta-Orchestrator.
- При В: указать ссылку на целевой репозиторий и оставить в data7 только резюме.
